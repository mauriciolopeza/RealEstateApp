@model IEnumerable<RealEstateApp.Models.Notifications>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Notification)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NotificationDatetime)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FkUsers)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Notification)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NotificationDatetime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FkUsers.LastNames)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.NotificationsId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.NotificationsId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.NotificationsId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
