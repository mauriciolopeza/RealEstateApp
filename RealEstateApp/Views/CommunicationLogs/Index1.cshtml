@model IEnumerable<RealEstateApp.Models.CommunicationLog>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CommunicationDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CommunicationType)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Notes)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CommunicationDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CommunicationType)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Notes)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CommunicationLogId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CommunicationLogId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CommunicationLogId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
